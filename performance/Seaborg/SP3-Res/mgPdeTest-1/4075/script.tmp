#!/usr/bin/csh
#@ job_name        = testExt-4075
#@ output          = data/testExt-4075.out
#@ error           = data/testExt-4075.err
#@ job_type        = parallel
#@ class           = regular_n3e 
#@ total_tasks     = 4075 
#@ node            = 255
#@ wall_clock_limit= 05:00:00
#@ notification    = complete
#@ network.MPI     = csss,not_shared,us
#@ queue
setenv MP_PIPE_SIZE 16 
poe+ ./run --commlib mpi --processes 4075 --nocomminit --debug 5
