1)  In parallel.cc:
    old bounding box;
2)  H_bounding nicht mit 2 multipizieren
3)  Parallele AVS Ausgabe

2)  VIelleicht bei parallel -> domain -> grobe Gitterberechnung
    benoetight man neighbour_non_periodic

11) grid/parallel.cc:  ind = ind.son(ENTd);

12) printavs.cc bei periodisch richtig machen

13) verschiebt nicht richtigf??
  D3vector move(0.25,0.25,0.0), scale(0.5,0.5,1.0);
  Domain domain((peri_cylinder*scale)+move);               

14) nochmal bei gridbase.cc [0,1]^3 schauen

