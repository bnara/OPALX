name: CI workflow
          
on:       
  push:   
  pull_request:
    branches: [ "main" ]
          
jobs:     
  # ===== CPU SERIAL BUILD =====
  build-cpu:  
    runs-on: ubuntu-latest
          
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
          
      - name: Install build tools and linter
        run: |
          sudo apt-get update
          sudo apt-get install -y build-essential cmake git libopenmpi-dev openmpi-bin

      - name: Run gen_OPALrevision
        run: |
          chmod +x ./gen_OPALrevision
          ./gen_OPALrevision
          
      - name: Configure CMake project
        run: |
          mkdir -p build
          cd build
          cmake .. \
            -DCMAKE_BUILD_TYPE=Debug \
            -DCMAKE_CXX_STANDARD=20 \
            -DIPPL_ENABLE_FFT=ON \
            -DIPPL_ENABLE_SOLVERS=ON \
            -DIPPL_ENABLE_ALPINE=OFF \
            -DIPPL_ENABLE_TESTS=OFF \
            -DIPPL_PLATFORMS=serial
          
      - name: Build project
        run: |
          cd build
          make -j$(nproc)
          
  # ===== GPU CUDA BUILD =====
  build-cuda:
    runs-on: ubuntu-latest
    container:
      image: nvidia/cuda:12.2.2-devel-ubuntu22.04  # CUDA-enabled container for compilation
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Install dependencies
        run: |
          apt-get update
          apt-get install -y build-essential cmake git libopenmpi-dev openmpi-bin

      - name: Run gen_OPALrevision
        run: |
          chmod +x ./gen_OPALrevision
          ./gen_OPALrevision

      - name: Configure CMake project (CUDA)
        run: |
          mkdir -p build
          cd build
          cmake .. \
            -DCMAKE_BUILD_TYPE=Debug \
            -DIPPL_PLATFORMS=CUDA \
            -DKokkos_ARCH_AMPERE80=ON \
            -DCMAKE_CXX_STANDARD=20 \
            -DIPPL_ENABLE_FFT=ON \
            -DIPPL_ENABLE_SOLVERS=ON \
            -DIPPL_ENABLE_ALPINE=OFF \
            -DIPPL_ENABLE_TESTS=OFF
          
      - name: Build project
        run: |
          cd build
          make -j$(nproc)
